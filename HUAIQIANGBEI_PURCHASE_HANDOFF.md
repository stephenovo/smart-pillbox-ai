# Smart Pillbox AI 华强北采购交接说明

## 项目背景

我们在做一个 Smart Pillbox AI 药盒 Demo。

目标是做一个可以现场演示的硬件 MVP：

- ESP32-S3 连接 Wi-Fi
- Web App / API 下发提醒状态
- 对应药盒格子的 LED 亮起
- 蜂鸣器响
- 用户打开某个药盒格子
- 传感器检测到“盒盖打开”
- ESP32 回传事件给 App
- App 显示服药/开盖完成

目前已经有：

- ESP32-S3 开发板
- 面包板
- 杜邦线
- 330 欧姆电阻
- LED
- 圆片磁铁
- 小药盒/小透明盒，每个小盒模拟一个 slot
- MacBook，可以用 Arduino CLI 上传程序
- OLED 可选，不是 MVP 必需

目前缺的主要是：

- 干簧管模块 / 磁簧开关模块
- 3.3V 有源蜂鸣器模块
- 可能需要新的 OLED，如果现场能买到可测试的

## 必买 1：干簧管模块 / 磁簧开关模块

### 作用

用于检测药盒盖子是否打开/关闭。

最终安装方式：

- 干簧管模块贴在药盒盒体边缘
- 圆片磁铁贴在对应盒盖上
- 盒盖关闭时，磁铁靠近干簧管
- 盒盖打开时，磁铁远离干簧管
- ESP32 通过数字信号判断 slot 是 OPEN 还是 CLOSED

### 推荐购买关键词

```text
干簧管模块
磁簧开关模块
3.3V
VCC GND DO
常开
Normally Open
ESP32 可用
```

### 最推荐型号/规格

优先买三针模块：

```text
VCC / GND / DO
```

要求：

- 支持 3.3V
- 数字输出 DO
- 常开型 NO / Normally Open
- 已焊排针
- 可以直接插杜邦线
- 适合 Arduino / ESP32

数量：

```text
至少 8 个
建议 10-12 个，留备用
```

### 四针模块也可以

如果只有四针也可以买：

```text
VCC / GND / DO / AO
```

我们只使用：

```text
VCC
GND
DO
```

AO 不用。

### 不建议买

不要买这些作为当前 MVP 的主方案：

```text
裸玻璃干簧管
LM393P 裸芯片
只有芯片没有模块的小元件
需要焊接才能用的版本
```

## 必买 2：3.3V 有源蜂鸣器模块

### 作用

用于提醒响铃。App 下发提醒后，ESP32 让蜂鸣器响，模拟药盒提醒用户吃药。

### 推荐购买关键词

```text
3.3V 有源蜂鸣器模块
ESP32 蜂鸣器模块
三针 有源蜂鸣器
VCC GND S
高电平触发
```

### 最推荐规格

```text
3针模块
VCC / GND / S
或 + / - / S
```

要求：

- 明确支持 3.3V
- 有源蜂鸣器
- ESP32 GPIO 可以控制
- 已焊排针
- 可以直接插杜邦线

数量：

```text
买 2-3 个
```

### 高电平触发 vs 低电平触发

优先买：

```text
高电平触发
```

原因是逻辑更直观：

```text
GPIO HIGH -> 响
GPIO LOW  -> 不响
```

如果只有低电平触发也可以，程序里可以改。

### 不建议买

不要买：

```text
只写 5V 的蜂鸣器模块
裸蜂鸣器
无源蜂鸣器，如果没有明确说明怎么驱动
```

我们现在手上有一个 5V 蜂鸣器，可以单独用 5V 测试响，但不适合直接接 ESP32 GPIO 长期驱动。

## 可选购买：OLED 屏幕

OLED 不是 MVP 必需，但可以提升现场展示效果。

### 作用

本地显示状态，比如：

```text
SMART PILLBOX
REMINDER SLOT 2
TAKE MEDICINE
DONE
```

即使没有 OLED，整个 Demo 也可以完成。

### 推荐购买关键词

```text
0.96寸 OLED
SSD1306
I2C
128x64
四针
GND VCC SCL SDA
3.3V 5V 兼容
已焊排针
```

### 推荐规格

```text
0.96 inch
128x64
SSD1306
I2C
4 pin: GND VCC SCL SDA
3.3V compatible
已焊接排针
```

### ESP32 当前接线约定

```text
OLED GND -> ESP32 GND
OLED VCC -> ESP32 3V3
OLED SCL -> ESP32 GPIO42
OLED SDA -> ESP32 GPIO41
```

### 注意

OLED 上电后黑屏不一定是坏的，因为 SSD1306 通常需要程序初始化后才显示。

如果现场测试，需要接 ESP32 并运行程序，而不是只接电源。

## 备用方案：如果买不到干簧管模块，可以用按钮模拟

可以。

如果买不到干簧管模块，我们可以用按钮临时模拟“打开盒盖”。

Demo 逻辑变成：

```text
按下按钮 = 用户打开/确认该药盒格子
松开按钮 = 默认状态
```

这不能真实检测磁铁和盒盖，但可以让 App + ESP32 + LED + 蜂鸣器 + 事件回传的完整流程跑通。

### 推荐按钮类型

优先买：

```text
轻触按键模块
按钮模块
3针
VCC GND OUT
3.3V 兼容
已焊排针
```

推荐关键词：

```text
轻触按键模块 3.3V
按钮模块 VCC GND OUT
ESP32 按键模块
微动开关模块
```

数量：

```text
至少 8 个
建议 10-12 个
```

### 最推荐按钮模块规格

```text
VCC / GND / OUT
3.3V compatible
数字输出
已焊排针
杜邦线可接
```

这种最适合新手免焊接。

### 如果没有模块，也可以买普通按钮

可以买：

```text
6x6x5mm 轻触开关
面包板可插
四脚轻触按钮
```

但这种需要我们用面包板和电阻/内部上拉来接线，调试会稍微麻烦一点。

如果只是为了 Demo 保险，优先买“按钮模块”，不要优先买裸按钮。

## 可选备用电子元件

这些不是第一优先，但很便宜，建议顺手买一点：

```text
S8050 或 2N2222 三极管
1k 欧姆电阻
10k 欧姆电阻
面包板跳线盒
杜邦线 公对公 / 公对母 / 母对母
小号扎带
3M 双面胶 / 泡棉胶
```

### 三极管、电阻用途

如果最后必须使用 5V 蜂鸣器，ESP32 不能直接驱动它。

这时可以用：

```text
ESP32 GPIO -> 1k 欧姆 -> 三极管基极
10k 欧姆下拉
三极管控制蜂鸣器供电
```

但如果买到了真正 3.3V 可用的有源蜂鸣器模块，这套暂时不需要。

## 采购优先级

最高优先级：

```text
1. 干簧管模块，3.3V，VCC/GND/DO，8-12个
2. 3.3V 有源蜂鸣器模块，2-3个
```

第二优先级：

```text
3. OLED SSD1306 I2C 128x64，4针，3.3V兼容，1-2个
4. 按钮模块，3.3V，VCC/GND/OUT，8-12个，作为干簧管买不到时的备用
```

顺手买：

```text
5. S8050 或 2N2222
6. 1k 欧姆电阻
7. 10k 欧姆电阻
8. 面包板跳线盒
9. 杜邦线
10. 双面胶/泡棉胶/扎带
```

## 给商家的直接说法

可以直接这样问商家：

```text
我要做 ESP32-S3 项目，需要免焊接模块。

第一，我要 8 到 12 个干簧管/磁簧开关模块，要支持 3.3V，最好是 VCC/GND/DO 三针，常开型，可以用磁铁触发。

第二，我要 2 到 3 个 3.3V 有源蜂鸣器模块，三针 VCC/GND/S，ESP32 GPIO 可以控制，最好高电平触发。

第三，如果有 OLED，我要 0.96寸 SSD1306 I2C 128x64 四针 GND/VCC/SCL/SDA，支持 3.3V，已焊排针。

第四，如果没有干簧管模块，我要 8 到 12 个 3.3V 按钮模块，VCC/GND/OUT 三针，用来模拟开盖。
```

## 最终判断标准

买到的模块最好都满足：

```text
支持 3.3V
有清楚丝印
有 VCC/GND/信号脚
已焊排针
可以直接插杜邦线
不需要焊接
ESP32/Arduino 可用
```

不要买那种只有芯片、没有模块、没有排针、必须焊接才能用的版本。
